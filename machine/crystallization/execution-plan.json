{
  "naturally_deployable": false,
  "test_commands": [["python", "-m", "pytest", "-q"]],
  "build_commands": [["python", "-m", "pip", "wheel", ".", "--no-deps", "-w", "dist"]],
  "runtime_commands": [["energy-alpha-plan", "--compact"], ["python", "scripts/operate.py"]],
  "truth_commands": [["python", "scripts/verify_public_core.py"]],
  "deployment_reason": "Local power-budget planner; grid/facility attachment requires separate telemetry, electrical authority, hardware, and safety evidence."
}
